info:
  name: users
  type: http
  seq: 1

http:
  method: GET
  url: https://localhost:7215/Users/Add
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
